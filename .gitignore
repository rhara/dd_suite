__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/

# dock_and_validate pipeline output (reproducible from committed fixture
# data via the commands in README.md)
data/out/

# scripts/install_all.py's per-machine record of what's actually installed
# where -- not something to commit, regenerate any time with the script
install_manifest.json
